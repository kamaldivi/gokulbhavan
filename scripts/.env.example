# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit it.

# ── Database (used only to read tbl_video_play_list) ──────────
DB_HOST=db5020426059.hosting-data.io
DB_NAME=dbs15655922
DB_USER=dbu3180665
DB_PASS=

# ── YouTube Data API v3 key ────────────────────────────────────
# Find it in tbl_global.youtube_api_key, or Google Cloud Console.
YOUTUBE_API_KEY=

# ── Output ─────────────────────────────────────────────────────
# Directory where the CSV will be written (default: current dir)
OUTPUT_DIR=.
