---
/**
 * Tamil page — gokulbhavan.org/tamil
 * Gitamrita (கீதா அமுதம்) program info + Tamil video strip.
 * Uses the standard main-site navigation.
 */
import Base from "@layouts/Base.astro";
import FeaturedVideos from "@components/FeaturedVideos.astro";
---

<Base
  title="தமிழ் | Gokul Bhavan"
  description="கீதா அமுதம் - A weekly Tamil Bhagavad-Gita study program by Gokul Bhavan Gaudiya Matha."
  activePath="/tamil"
>

  <!-- Hero banner — Gitamrita launch -->
  <section class="relative w-full bg-brand-navy" style="min-height: 460px;" aria-label="Gitamrita launch banner">
    <div class="absolute inset-0 bg-cover bg-center"
         style="background-image: url('/assets/gb_banner_tamil.png');" aria-hidden="true"></div>
    <!-- Gradient: light at top (preserve image sky), dark at bottom (text legibility) -->
    <div class="absolute inset-0"
         style="background: linear-gradient(to bottom, rgba(0,0,0,0.35) 0%, rgba(0,0,0,0.85) 100%);"
         aria-hidden="true"></div>

    <div class="relative z-10 flex flex-col items-center justify-end text-center px-6 pb-12"
         style="min-height: 460px;">

      <!-- Launch badge -->
      <span class="inline-block bg-brand-lotus text-white text-[11px] font-bold uppercase tracking-widest px-4 py-1 rounded-full mb-5 shadow">
        விரைவில் தொடங்குகிறது · Launching Soon
      </span>

      <!-- Tamil title -->
      <p class="font-tamil text-brand-gold text-2xl md:text-3xl font-semibold mb-1 drop-shadow">
        கீதா அமுதம்
      </p>

      <!-- English title -->
      <h1 class="font-display font-bold text-3xl md:text-4xl lg:text-5xl text-white leading-tight mb-3 drop-shadow-md">
        Gita Amutham
      </h1>

      <!-- Tagline -->
      <p class="font-tamil text-white/85 text-base md:text-lg italic mb-1 drop-shadow">
        கீதையைக் கேட்போம் · அதன் பாதையில் நடப்போம்
      </p>
      <p class="text-white/70 text-sm mb-6 drop-shadow">
        "Let us hear the Gita and walk on its path"
      </p>

      <!-- Program snapshot -->
      <div class="flex flex-wrap items-center justify-center gap-3 mb-7 text-xs font-semibold text-white/80">
        <span id="tamil-day-chip" class="bg-white/15 backdrop-blur px-3 py-1.5 rounded-full">📅 Every Friday</span>
        <span id="tamil-time-chip" class="bg-white/15 backdrop-blur px-3 py-1.5 rounded-full">🕐 10:30 AM EST</span>
        <span class="bg-white/15 backdrop-blur px-3 py-1.5 rounded-full">⏱ 95 minutes</span>
      </div>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row gap-3 justify-center flex-wrap">
        <a href="/register"
           class="inline-flex items-center justify-center bg-brand-lotus hover:bg-pink-700 text-white font-semibold px-7 py-3 rounded-full transition-colors shadow">
          பதிவு செய்யுங்கள் · Register
        </a>
        <a href="/programs"
           class="inline-flex items-center justify-center border-2 border-white text-white hover:bg-white hover:text-brand-navy font-semibold px-7 py-3 rounded-full transition-colors">
          நிகழ்ச்சிகள் காண · Explore Programs
        </a>
        <a href="https://us02web.zoom.us/j/2294369627?pwd=VHJPQ1dBTC9wVnV2dUd5UDVMYTJwQT09"
           target="_blank" rel="noopener noreferrer"
           class="inline-flex items-center gap-2 bg-brand-navy hover:bg-brand-slate text-white font-semibold px-7 py-3 rounded-full transition-colors shadow">
          <svg class="w-4 h-4 fill-current shrink-0" viewBox="0 0 24 24">
            <path d="M24 12c0 6.627-5.373 12-12 12S0 18.627 0 12 5.373 0 12 0s12 5.373 12 12zm-6.508-4.5l-3.743 2.688V7.5H6.257C5.563 7.5 5 8.063 5 8.757v6.985h8.249c.694 0 1.257-.563 1.257-1.257V12l3.743 2.7A.375.375 0 0 0 19 14.4V7.8a.375.375 0 0 0-.508-.3z"/>
          </svg>
          Join on Zoom
        </a>
        <a href="https://www.youtube.com/@bkdasa"
           target="_blank" rel="noopener noreferrer"
           class="inline-flex items-center gap-2 bg-red-600 hover:bg-red-700 text-white font-semibold px-7 py-3 rounded-full transition-colors shadow">
          <svg class="w-4 h-4 fill-current shrink-0" viewBox="0 0 24 24">
            <path d="M23.495 6.205a3.007 3.007 0 0 0-2.088-2.088c-1.87-.501-9.396-.501-9.396-.501s-7.507-.01-9.396.501A3.007 3.007 0 0 0 .527 6.205a31.247 31.247 0 0 0-.522 5.805 31.247 31.247 0 0 0 .522 5.783 3.007 3.007 0 0 0 2.088 2.088c1.868.502 9.396.502 9.396.502s7.506 0 9.396-.502a3.007 3.007 0 0 0 2.088-2.088 31.247 31.247 0 0 0 .5-5.783 31.247 31.247 0 0 0-.5-5.805zM9.609 15.601V8.408l6.264 3.602z"/>
          </svg>
          Watch on YouTube
        </a>
      </div>

    </div>
  </section>

  <!-- Sanga flyer -->
  <section class="max-w-6xl mx-auto px-4 sm:px-6 py-10">
    <img src="/assets/tamil_sanga_flyer_1.jpeg"
         alt="கீதா அமுதம் — Tamil Sanga Flyer"
         class="w-full rounded-2xl shadow-lg border border-brand-tan" />
  </section>

  <!-- Tamil video strip -->
  <div class="max-w-6xl mx-auto px-4 sm:px-6 border-t border-brand-tan">
    <FeaturedVideos
      id="tamil-latest"
      title="Recent Harikathas - Tamil"
      apiParams="category=Harikatha-Tamil&limit=5&sort=published_date"
      moreLink="/videos"
      moreLinkLabel="More videos →"
      mode="strip"
    />
  </div>

</Base>

<script>
// Gita Amrutam runs every Friday at 10:30 AM America/New_York.
// Convert to the visitor's browser timezone and update the hero chips.
(function () {
  // --- same conversion helper as programs.astro ---
  function etToLocal(srcDay: string, timeEst: string) {
    const DAYS = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];
    const srcIdx = DAYS.indexOf(srcDay);
    // Accept both "7:00 PM" (legacy) and "19:00" (HTML time input, 24-hour)
    const m12 = timeEst.match(/^(\d{1,2}):(\d{2})\s*(AM|PM)$/i);
    const m24 = timeEst.match(/^(\d{1,2}):(\d{2})$/);
    if (srcIdx === -1 || (!m12 && !m24)) return null;
    let h: number, min: number;
    if (m12) {
      h = parseInt(m12[1]); min = parseInt(m12[2]);
      if (m12[3].toUpperCase() === 'PM' && h !== 12) h += 12;
      if (m12[3].toUpperCase() === 'AM' && h === 12) h = 0;
    } else {
      h = parseInt(m24![1]); min = parseInt(m24![2]);
    }

    const now  = new Date();
    const diff = (srcIdx - now.getDay() + 7) % 7;
    const ref  = new Date(now);
    ref.setDate(now.getDate() + diff);

    const probe = new Date(Date.UTC(ref.getFullYear(), ref.getMonth(), ref.getDate(), h, min));
    const etHourRaw = parseInt(
      new Intl.DateTimeFormat('en-US', { timeZone: 'America/New_York', hour: 'numeric', hour12: false }).format(probe)
    );
    const etHour = etHourRaw === 24 ? 0 : etHourRaw;
    let offsetH  = h - etHour;
    if (offsetH > 12)  offsetH -= 24;
    if (offsetH < -12) offsetH += 24;
    const utcDate = new Date(probe.getTime() + offsetH * 3_600_000);

    const dayFull = new Intl.DateTimeFormat('en-US', { weekday: 'long' }).format(utcDate);
    const time    = new Intl.DateTimeFormat('en-US', { hour: 'numeric', minute: '2-digit', hour12: true }).format(utcDate);
    const tzAbbr  = new Intl.DateTimeFormat('en-US', { timeZoneName: 'short' })
      .formatToParts(utcDate).find((p: Intl.DateTimeFormatPart) => p.type === 'timeZoneName')?.value ?? '';
    const dayChanged = dayFull !== srcDay;

    return { dayFull, time, tzAbbr, dayChanged };
  }

  const lt = etToLocal('Friday', '10:30'); // stored as 24-hour by HTML time input
  if (!lt) return;

  const dayChip  = document.getElementById('tamil-day-chip');
  const timeChip = document.getElementById('tamil-time-chip');

  if (dayChip) {
    // If day flips (e.g. Friday ET → Saturday IST), show the local day
    dayChip.textContent = lt.dayChanged
      ? `📅 Every ${lt.dayFull}`
      : '📅 Every Friday';
  }
  if (timeChip) {
    timeChip.textContent = `🕐 ${lt.time} ${lt.tzAbbr}`;
  }
})();
</script>
